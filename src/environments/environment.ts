// This file can be replaced during build by using the `fileReplacements` array.
// `ng build ---prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
    firebase: {
        apiKey: "AIzaSyDR01fxU-v6whOGpi8CB5HhfOPJPrZf7rY",
        authDomain: "invoicemanagement-fa34a.firebaseapp.com",
        databaseURL: "https://invoicemanagement-fa34a.firebaseio.com",
        projectId: "invoicemanagement-fa34a",
        storageBucket: "invoicemanagement-fa34a.appspot.com",
        messagingSenderId: "654986020566"

    }
};

/*
 * In development mode, for easier debugging, you can ignore zone related error
 * stack frames such as `zone.run`/`zoneDelegate.invokeTask` by importing the
 * below file. Don't forget to comment it out in production mode
 * because it will have a performance impact when errors are thrown
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.

